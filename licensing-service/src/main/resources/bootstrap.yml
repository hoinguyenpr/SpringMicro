#Licensing Server get configuration from Configuration Server for boostrap
spring:
        application:
                name: licensing-service
        profiles:
                active: dev
        config:
                import: optional:configserver:http://localhost:8071/